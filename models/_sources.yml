version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: orders
        columns: 
          - name: id
            tests:
              - unique
      - name: customers


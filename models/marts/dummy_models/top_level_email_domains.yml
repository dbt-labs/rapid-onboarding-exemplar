models:
  - name: top_level_email_domains
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: tld
        tests:
          - not_null
      - name: email_provider
        tests:
          - not_null
          - accepted_values:
              values:
                - 'yahoo'
                - 'google'
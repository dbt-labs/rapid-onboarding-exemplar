
Where to find in built test case definitions ?

    - https://docs.getdbt.com/reference/resource-properties/tests

Nice to see someone implement this:

    - Create a test case to validate the join condition of left v/s inner

    - If left join results in different counts compared to inner join

Auto-timeout for each job - 10mins!!

    - If this fails, it means the query logic is at fault or use limited dataset

How do we git pull origin master (or other branch) before committing from the UI? 